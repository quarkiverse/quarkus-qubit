/**
 * Internal runtime implementation classes for Qubit query execution.
 *
 * <p>
 * All types in this package are non-null by default.
 * Parameters, return values, and fields accept and return non-null
 * values unless explicitly annotated with {@code @Nullable}.
 *
 * <p>
 * <strong>Internal API — not for public consumption.</strong>
 *
 * @since 1.0
 */
@NullMarked
package io.quarkiverse.qubit.runtime.internal;

import org.jspecify.annotations.NullMarked;
