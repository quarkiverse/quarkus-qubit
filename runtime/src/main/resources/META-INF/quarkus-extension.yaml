name: Qubit
description: |
  Type-safe JPA Criteria queries using lambda expressions.
  Query entities using fluent, compile-time checked syntax.
metadata:
  keywords:
    - jpa
    - criteria
    - criteria-api
    - lambda
    - panache
    - query
    - typesafe
    - type-safe
    - orm
    - hibernate
    - build-time
  guide: https://docs.quarkiverse.io/quarkus-qubit/dev/
  categories:
    - "data"
  status: "preview"
  config:
    - "quarkus.qubit."
  codestart:
    name: qubit
    languages:
      - java
    artifact: "io.quarkiverse.qubit:quarkus-qubit:codestarts:jar:${project.version}"
